management:
  metrics:
    tags:
      application: "auth"
  endpoints:
    web:
      exposure:
        include: "health"
      base-path: "/"
      path-mapping:
        health: "/health"
  endpoint:
    health:
      enabled: true
      show-components: always
      probes:
        enabled: true
